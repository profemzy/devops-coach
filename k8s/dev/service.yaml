apiVersion: v1
kind: Service
metadata:
  name: wackops-coach
  namespace: wackops-coach
spec:
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: wackops-coach
